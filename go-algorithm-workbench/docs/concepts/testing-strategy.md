# Testing strategy

Data-structure tests should exercise empty state, a single value, duplicate
values, and growth. Algorithm tests should include degenerate input and verify
results, not incidental implementation order unless that order is contractual.
