# Graph algorithms

Use BFS for unweighted shortest paths and DFS for reachability or recursive
structure discovery. Dijkstra requires non-negative edge weights. A
topological ordering exists only in a directed acyclic graph; the API reports
failure when it detects a cycle.
