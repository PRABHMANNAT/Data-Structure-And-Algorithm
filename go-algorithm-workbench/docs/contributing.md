# Contributing

Keep APIs small and generic where Go permits it naturally. Add unit tests for
normal operation plus boundary cases, run `go fmt ./...`, then run
`go test ./...` before submitting a change. Document complexity whenever a
public operation is introduced.
