# Go Search Structures

An advanced, dependency-free search-index project built from a Bloom filter, prefix trie, inverted postings, LRU cache, and top-k ranking.

Run `go test ./...` or `go run ./cmd/demo`.
