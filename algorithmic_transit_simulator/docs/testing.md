# Testing strategy

Every data structure has focused unit coverage for its key invariant: heap stability, AVL balance, union-find merge behavior, trie ordering, interval overlap, and cache eviction.

Integration tests cover graph loading, CLI output, service composition, earliest-arrival routing, disruption projection, and event simulation. Run all tests from the repository root with the command shown in the project README.
