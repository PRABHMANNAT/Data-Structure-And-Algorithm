# Linear collections

A stack processes the most recently added value first. A queue processes the earliest added value first. A deque combines both choices and is useful for sliding-window algorithms.
