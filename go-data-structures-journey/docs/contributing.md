# Contributing

Keep implementations dependency-free, document non-obvious complexity tradeoffs, and add a focused test for every behavior change. Use conventional commit messages so the history remains easy to scan.
