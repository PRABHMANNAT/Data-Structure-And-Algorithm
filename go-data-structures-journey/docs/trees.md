# Trees and heaps

The binary search tree keeps values ordered for in-order traversal. The priority queue uses a binary heap, which is optimized for repeatedly retrieving the smallest element rather than fully sorting every value.
