# Graph structures

The graph stores directed edges as adjacency sets. Breadth-first traversal visits every reachable vertex layer by layer. Disjoint sets efficiently answer whether two elements share a component.
