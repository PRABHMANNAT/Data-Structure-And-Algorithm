# Testing approach

Each structure has a narrowly scoped unit test that demonstrates its primary invariant: order, membership, capacity, traversal, or eviction. Run the complete suite with `go test ./...`.
