# Linked lists

Linked lists trade cache locality for cheap endpoint updates. The singly linked list is useful for simple forward traversal; the doubly linked list also supports efficient removal from the tail.
