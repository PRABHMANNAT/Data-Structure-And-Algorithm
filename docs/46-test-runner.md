# Test runner

The repository includes a small assertion-based test executable that can be compiled with the core library. Future CMake integration can register it with CTest as the test surface grows.
