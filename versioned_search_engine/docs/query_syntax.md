# Query syntax

Terms are normalized to lowercase alphanumeric tokens. Use `AND`, `OR`, and `NOT` to combine postings; the lightweight parser also recognizes parentheses for future expression grouping. Exact adjacent phrase matching is available separately through the positional index.
