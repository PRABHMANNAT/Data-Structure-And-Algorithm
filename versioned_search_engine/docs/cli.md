# Command-line search

Run the CLI from inside the project directory:

```bash
python -m search_engine.cli data/sample_documents.json rail
```

Each result line contains a document ID and its BM25 score.
