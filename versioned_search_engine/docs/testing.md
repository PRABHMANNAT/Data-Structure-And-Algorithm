# Test strategy

Unit tests exercise the invariants of each data structure: sorted postings, prefix order, top-k eviction, snapshot isolation, and Bloom-filter membership. Integration tests cover corpus loading, CLI rendering, phrase matches, Boolean operations, and ranking cache invalidation.
